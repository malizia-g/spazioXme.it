---
layout: layouts/page.njk
title: "Crediti immagini"
description: "Informazioni sulle fotografie utilizzate sul sito di SpazioxMe."
noindex: true
breadcrumb:
  - text: Home
    url: /
  - text: Crediti immagini
    url: /crediti/
---
<section class="sezione container">
  <div class="articolo__corpo" style="margin-inline:0;max-width:60rem">
    <h1>Crediti immagini</h1>
    <p>
      Le fotografie pubblicate sul sito sono <strong>immagini originali di SpazioxMe</strong> e
      ritraggono gli ambienti reali dello spazio: la sala principale, l'area meditazione, gli
      angoli relax e i dettagli che lo rendono un luogo accogliente.
    </p>
    <p>
      Tutte le foto sono di proprietà di SpazioxMe. Per riutilizzarle, anche parziale, scrivici
      dalla <a href="/contatti/">pagina contatti</a>.
    </p>
    {% if crediti and crediti.length %}
    <div style="overflow-x:auto">
      <table style="width:100%;border-collapse:collapse;font-size:var(--fs-sm)">
        <thead>
          <tr style="text-align:left;border-bottom:2px solid var(--colore-bordo)">
            <th style="padding:.5rem">Immagine</th>
            <th style="padding:.5rem">Autore</th>
            <th style="padding:.5rem">Licenza</th>
          </tr>
        </thead>
        <tbody>
          {% for c in crediti %}
          <tr style="border-bottom:1px solid var(--colore-bordo)">
            <td style="padding:.5rem"><a href="{{ c.pagina }}" rel="noopener" target="_blank">{{ c.titolo }}</a></td>
            <td style="padding:.5rem">{{ c.autore }}</td>
            <td style="padding:.5rem"><a href="{{ c.licenzaUrl }}" rel="noopener license" target="_blank">{{ c.licenza }}</a></td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
    {% endif %}
  </div>
</section>
